// Shared glass recipe for novel-shell panels (rails + artifact stage).
// Callers own layout (width, padding, flex) via className; this owns the look.
export const NOVEL_SHELL_GLASS_PANEL
  = 'rounded-[16px] border border-[var(--nw-glass-border)] bg-[var(--nw-glass-bg)] backdrop-blur-[24px] shadow-[var(--nw-copilot-panel-shadow)]'
